enum RequestState {
  idle,
  loading,
  success,
  failed,
}
enum PaymentPLAN { free,month, halfyear,year }
enum PaymentMethod { nopay,stripe, paypal }
enum Onboardseenenum { NotSeen,Seen }
enum verifiedenum{Verify,notverify}


